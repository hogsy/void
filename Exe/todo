BUGS:

Client info doesnt propagate across levels
Problems when reconnecting sometimes.
Client Socket should be the same



GAME
========
add animation cycle flags

entities
========

add delay var to sound sources
add renderering flags to model entities
add frame range to model entities
